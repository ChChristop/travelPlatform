# Phase 0 corrective task — Codex review

Initial audit: CORRECTION REQUIRED. Rewrite both audit documents using these corrections as higher priority than the original prompt's stale build status. Application code remains read-only. Do not begin Phase 1.

1. dayGroups uses literal start.slice(0,10), NOT UTC conversion. For 2026-09-27T00:30:00+09:00 the key is 2026-09-27. Current +09:00 fixtures are consistent. Risk arises when migrating to Z/other-offset timestamps, cross-midnight lodging, and project timezones. Timeline fmtDay omits explicit timezone. SIM input displays browser-local wall time but parses +09:00; issue outside JST-equivalent browser zones. Future timezone must be project.timezone, not all trips JST.
2. Do NOT convert now/simTime into TripRun/ItemExecution: those are clocks, not evidence of actual execution. LIVE currently has no execution recording; future TripRun requires explicit lifecycle, SIM remains independent preview.
3. Missing sections MUST add: Route/Page structure (single root, no router, no separate routes/pages, LIVE/SIM state toggle, checklist conditional drawer); build/Pages (vite base './', dist, no .github workflow despite README claim, no verified deployed site); exact component/data dependencies and state ownership.
4. LIVE findState is array-first not independent sorting. upcoming uses first end > time and is passed as ClockPanel.next, so ongoing event appears as next with negative countdown, current fallback effectively unreachable during an event. Preparation only static EventDetail text; no timed notification trigger. ClockPanel computes time variable but never renders it separately (date formatting can include time via fmt defaults).
5. plannedPosition returns current coordinate, else interpolates prev-end to next-start if both have coordinates, else current/next/prev first coordinate. Straight lines are not real transport routing. Map markers use active time date, not selected item's date; selected can focus another day. Zoom disables follow only with originalEvent. Each now change redraws layers. GPS is one-shot button request (no watchPosition/history/delay detection); may still display in SIM, follow does not target GPS.
6. Correct fields: query is stored but never consumed by UI/geocoder. typeOf returns label/type/color, icons independently in Icons.TypeIcon. Unused import from trip.js to Icons couples data to JSX even though TypeIcon unused. Defaults note/prep empty strings, prepMinutes=0; optional coordinates/query/price/reservation/transport undefined. Todos priority stored but not sorted/used. App renders bare TodoList hiding progress. Date count Header=5 elapsed days versus Timeline=6 calendar dates.
7. Migration is lossless: don't remove fields, invent coordinates, infer bookings from reservation recommendation strings, coerce price ranges to exact numeric amounts, infer Place identity by coords alone, or overwrite corrupt storage with defaults. Preserve raw text/provenance and recoverable backup before versioned write; namespace by project; map legacy todo IDs explicitly. Target Place has no query field: keep legacy search text in migration metadata or deliberate extension, coordinates optional (not null per baseline interface). BookingPolicy recommendation != actual confirmed Booking. Check-in events != full lodging period; infer cautiously with documented mapping. transport string != complete Route origin/destination geometry. Keep existing UI ViewModel adapter. No implementation now.
8. Include verified data baseline from WORKLOG (46 events, six dates, 40 coordinates, six missing IDs, eight todos). Installed versions and environment from WORKLOG. Build PASS after EPERM retry; no lint/test scripts/typecheck/framework configured. No browser visual/GPS/deploy test claimed.
9. Design risk rank components with reasons: App grid/drawer/map lifecycle, styles.css fixed viewport and 1024/760 responsive layouts, Timeline active/selected/past & icons, ClockPanel controls, EventDetail content, Header hardcoded Kansai, TodoList bare drawer, MapPanel ref/height. User-specific edit provenance cannot be inferred.
10. HANDOFF must have actual file list (CURRENT_TASK, CURRENT_ARCHITECTURE, HANDOFF, WORKLOG, invoke-audit.ps1, REVIEW_CORRECTIONS), no invented +1000 line counts or optional files. AGENT_WORKFLOW.md externally modified during audit, preserve it and exclude from our changes. No commit/push without final review. Phase 1 proposal only Domain types with isolated TS static checking; no UI/store migration. Record Qwen API success; OpenCode desktop present but CLI/provider connection unverified.
