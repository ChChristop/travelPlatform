# R3-A corrective task

Your migration only produces bookkeeping (provenance/report/idMap) — it never actually constructs the migrated domain data. `src/domain/migrations/types.ts` has been corrected by Codex to require a `seed` field on `MigrationResult` (already updated on disk, re-read it). This is a serious gap: EXECUTION_SPEC.md requires the migration's output to be "신규 seed + legacy ID 대응표 + unresolved/provenance report" — the seed (actual entities) was completely missing, not just under-detailed.

Also: your current notes CLAIM things that never happen. For `rakukanki`, the note says "Reservation confirmed... Booking created." but no `Booking` object is ever constructed anywhere in your code. This is exactly the kind of fabrication CURRENT_TASK.md forbids ("날조 금지") — never describe an action in a note/report that the code doesn't actually perform.

## What to build (`migrate.ts`, return the new `seed` field matching `MigrationSeed` from `types.ts`)

Keep everything you already have (provenance, unresolved, report, idMap, deterministic hashing) — just add real entity construction alongside it.

1. **One `Workspace`, one `TravelProject`, one `PlanVersion`** for this whole migration (this is a single trip, single workspace/project). Use deterministic ids derived from a fixed seed string (e.g. `'kansai-2026'`), not random. `Workspace.projectIds`/`templateIds`/`collectionIds` can be `[]`/`[projectId]` as appropriate. `TravelProject.timezone: 'Asia/Tokyo'`, `status`/`visibility` pick reasonable fixed values (e.g. `'planning'`, `'private'`) — these are structural placeholders, not claims about the real project's status, say so in a top-level report note if you add one.
2. **`Place[]`**: for every legacy event with valid `lat`/`lng`, create one `Place` (`workspaceId` = the single workspace's id, `id` deterministic from lat+lng rounded to ~5 decimals so identical coordinates dedupe into the same Place, `name` from the event's `title` or `city` — pick one and be consistent). Events sharing the same rounded coordinates must resolve to the *same* Place id, not duplicates. Events without coordinates get no Place (skip — do not invent coordinates).
3. **`PlanItem[]`**: for every valid legacy event, build a real discriminated-union `PlanItem` (`id` = deterministic from legacy id e.g. `plan-item:${legacyId}` cast to `PlanItemId`, `planVersionId`, `type` = your existing `TYPE_MAP` result, `title`, `schedule: {start, end}`, `places`: `[{placeId, role:'primary'}]` if a Place was created for this event in step 2, else `[]`, `detail: {type: mappedType}` — minimal detail is fine per R1, do not invent fields like `roomType` that aren't in the legacy data, `status: 'planned'`). The `type`/`detail.type` MUST match (this is a compiler-enforced discriminated union from R1 — if this doesn't type-check, you built it wrong).
4. **`Task[]`**: for every legacy todo, build a real `Task` (`id` deterministic e.g. `task:${legacyId}`, `projectId`, `title` = the todo's `label`, `status: 'todo'`). No `Trigger` needed unless you can derive one honestly from the todo's `date` (optional, skip if unsure).
5. **`Booking[]`**: ONLY when `isConfirmedReservation(e.reservation)` is true, construct one real `Booking` (`id` deterministic e.g. `booking:${legacyId}`, `projectId`, `planItemIds: [thePlanItemId]`, `type`: pick the closest `BookingType` from context (e.g. a restaurant event → `'restaurant'`, a hotel → `'hotel'`), `status: 'confirmed'`). If you cannot confidently pick a `BookingType`, do not fabricate one — fall back to not creating a Booking and change the note to say why, don't claim "Booking created" in that case.
6. Every `ProvenanceEntry.newIds` must list ALL new ids actually created for that legacy item (e.g. a confirmed-reservation event's `newIds` includes both its `plan-item:` id and its `booking:` id).
7. Notes must only describe what the code actually did — no note may say something was created unless it is genuinely present in the returned `seed` arrays. Re-check every note string against the actual arrays before finishing.

Do not weaken `src/domain/*` types, do not use `any`, do not cast whole objects. Run `scope` then `migrationRun`, re-read the generated `docs/migration/KANSAI_REPORT.md` and verify by eye that a few notes claiming Booking/Place creation actually correspond to entries in the seed. Write an accurate handoff (state plainly that the prior version had no seed at all) and finish.
